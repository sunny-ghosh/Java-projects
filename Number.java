import even.Calculate;                          // Using the user defined package from another location.
public class Number 
{
    public static void main(String args[])
    {
        Calculate ob = new Calculate();
        ob.check(5);
    }    
}
